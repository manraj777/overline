export const DOMAIN_CONFIG = {
  appUrl: process.env.APP_URL || 'https://overline.in',
  adminUrl: process.env.ADMIN_URL || 'https://admin.overline.in',
  apiUrl: process.env.API_URL || 'https://api.overline.in',
  mailFrom: process.env.MAIL_FROM || 'no-reply@overline.in',
  supportEmail: process.env.SUPPORT_EMAIL || 'support@overline.in',
};
