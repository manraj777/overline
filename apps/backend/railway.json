{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Build skipped - will build at startup'"
  },
  "deploy": {
    "startCommand": "npx prisma generate && node dist/main.js",    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
