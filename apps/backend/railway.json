{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm exec prisma generate && pnpm exec nest build && ls -la dist/"
  },
  "deploy": {
    "startCommand": "pnpm exec prisma generate && pnpm exec nest build && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
