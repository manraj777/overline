{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -f package-lock.json ]; then npm ci; else cd apps/admin-web && npm ci; fi",
  "buildCommand": "if [ -f package.json ] && grep -q '\"name\": \"@overline/admin-web\"' package.json; then npm run build; else cd apps/admin-web && npm run build; fi",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://overlinebackend-production.up.railway.app/api/:path*"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        }
      ]
    }
  ]
}
